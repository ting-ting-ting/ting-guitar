import React from 'react';
import './index.scss';

const ChordSample = ({

}) => {
  return (
    <div className="chord-sample">
      ChordSample
    </div>
  );
};

export default React.memo(ChordSample);
